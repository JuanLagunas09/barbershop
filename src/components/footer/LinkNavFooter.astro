---
const { label, link, isBlank= false } = Astro.props;
---

<a href={link} target={isBlank ? '_blank' : ''} class="text-button-color font-semibold hover:text-font-color opacity-90 transition-all duration-300 ease-in-out">
  <span>{label}</span>
</a>
